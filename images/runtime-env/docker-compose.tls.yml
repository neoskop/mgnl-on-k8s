version: "3.4"
services:
  sut:
    build:
      dockerfile: ../Dockerfile
      context: build
    environment:
      LOG_LEVEL: DEBUG
      MAGNOLIA_CONFIG: foo
      DATASOURCES: |
        {
          "datasources": [
            {
              "name": "MagnoliaAuthor",
              "database": "author",
              "host": "db",
              "password": "<&%!^>",
              "username": "author",
              "useSsl": true,
              "trustStore": "/tmp/truststore.jks",
              "trustStorePassword": "changedit",
              "enabledTLSProtocols": "TLSv1.2"
            }
          ]
        }
    volumes:
      - ./truststore.jks:/tmp/truststore.jks:ro
  db:
    image: mysql:9.7.1@sha256:e370cd5f64599d46985b7729b452f2153825246f88d82753ec595c5dfc6fef6a
    environment:
      MYSQL_DATABASE: author
      MYSQL_USER: author
      MYSQL_PASSWORD: <&%!^>
      MYSQL_RANDOM_ROOT_PASSWORD: "true"
    command:
      - --bind-address=0.0.0.0
      - --require_secure_transport=ON
      - --ssl
      - --default_authentication_plugin=mysql_native_password
      - --tls_version=TLSv1.2
