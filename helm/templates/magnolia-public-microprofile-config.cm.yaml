{{- $mergedConfig := include "mgnl.public.microprofileConfig" . | fromYaml -}}
{{- if $mergedConfig -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mgnl.public.name" . }}-microprofile-config
  labels:
    {{- include "mgnl.labels" . | nindent 4 }}
data:
  microprofile-config.yaml: |
    {{- toYaml $mergedConfig | nindent 4 }}
{{- end -}}
