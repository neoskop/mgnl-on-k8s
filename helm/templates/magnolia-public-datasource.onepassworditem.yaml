{{- if .Values.magnoliaPublic.datasource.onePassword.enabled }}
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ include "mgnl.public.name" . }}-datasource-password
  labels:
{{ include "mgnl.labels" . | indent 4 }}
spec:
  itemPath: "vaults/{{ .Values.magnoliaPublic.datasource.onePassword.vault }}/items/{{ .Values.magnoliaPublic.datasource.onePassword.item }}"
{{- end }}
