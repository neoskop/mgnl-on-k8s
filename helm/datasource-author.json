{
    "datasources": [
        {
            "host": "{{ include "mgnl.mysql.author.host" . }}",
            "port": "{{ .Values.magnoliaAuthor.datasource.port }}",
            "username": "{{ include "mgnl.mysql.author.username" . }}",
            "password": "{{ .Values.magnoliaAuthor.datasource.password }}",
            "name": "{{ .Values.magnoliaAuthor.datasource.name }}",
            "database": "{{ include "mgnl.mysql.author.database" . }}"
        }
    ]
}