{
    "datasources": [
        {
            "host": "{{ include "mgnl.mysql.public.host" . }}",
            "port": "{{ include "mgnl.public.datasource.port" . }}",
            "username": "{{ include "mgnl.mysql.public.username" . }}",
            "password": "${MGNL_PUBLIC_DB_PASSWORD}",
            "name": "{{ .Values.magnoliaPublic.datasource.name }}",
            "database": "{{ include "mgnl.mysql.public.database" . }}"
        }
    ]
}
